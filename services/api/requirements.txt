# Python 3.10–3.12 (ML wheels not yet on 3.14). The Docker image handles this;
# for local dev:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# SYSTEM DEPENDENCY (not pip): ffmpeg — decodes the live HLS stream reliably
# (OpenCV's HLS reader stalls). macOS: brew install ffmpeg · Debian/Ubuntu: apt install ffmpeg

# stream + capture
yt-dlp>=2024.1.0
opencv-python-headless>=4.8.0   # use opencv-python if you want cv2.imshow windows
numpy>=1.24.0

# detection + tracking
ultralytics>=8.3.0              # yolov8s; can export to OpenVINO for Intel CPUs
lapx>=0.5.0                     # linear assignment, required by ByteTrack

# species classification
ai-edge-litert>=1.0.0           # TFLite runtime for the iNat bird model

# live web app
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
requests>=2.31.0

# BioCLIP — the live species classifier (fine-grained, restricted to local species)
open-clip-torch>=2.24.0
